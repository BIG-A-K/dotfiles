# mercury — starship mercury（#107dc8 のブルーアクセント）
#
# tmux.conf から source-file される色定義。tmux.conf 単体でも動くように、
# ここでは配色に関する設定だけを上書きする。
# 生成元のパレットは starship.conf/mercury.toml を参照。
#
#   accent  #107dc8  starship の左端バッジ色。セッション名・時計・アクティブペイン枠
#   dir     #a3aed2  $directory の色。アクティブウィンドウ
#   git     #394260  $git_branch の背景。右側のホスト名ブロック
#   lang    #212736  言語セグメントの背景。非アクティブペイン枠
#   bar     #1d2230  ステータスバーの背景
#   text    #a0a9cb  通常の文字 / muted #6b7390  非アクティブの文字

# ステータスバー本体
set -g status-style "fg=#a0a9cb,bg=#1d2230"

# 左: セッション名を starship の左端バッジと同じ色にして powerline 区切りで繋ぐ
set -g status-left "#[fg=#ffffff,bg=#107dc8,bold] #S #[fg=#107dc8,bg=#1d2230,nobold]"
set -g status-left-length 40

# 右: ホスト名:ユーザー名（git セグメント色）＋ 日付(曜日) 時刻（アクセント色）
set -g status-right "#[fg=#394260,bg=#1d2230]#[fg=#a0a9cb,bg=#394260] #h:#(whoami) #[fg=#107dc8,bg=#394260]#[fg=#ffffff,bg=#107dc8,bold] %Y-%m-%d(%a) %H:%M "
set -g status-right-length 60

# ウィンドウ一覧: 非アクティブは沈ませ、アクティブだけ $directory の色で浮かせる
set -g window-status-separator ""
setw -g window-status-format "#[fg=#6b7390,bg=#1d2230] #I:#W#F "
setw -g window-status-current-format "#[fg=#a3aed2,bg=#1d2230]#[fg=#10151d,bg=#a3aed2,bold] #I:#W#F #[fg=#a3aed2,bg=#1d2230,nobold]"
setw -g window-status-activity-style "fg=#a3aed2,bg=#1d2230"
setw -g window-status-bell-style "fg=#107dc8,bg=#1d2230,bold"

# ペイン枠。アクティブなペインだけアクセント色にする
set -g pane-border-style "fg=#212736"
set -g pane-active-border-style "fg=#107dc8"
set -g pane-border-format "#[fg=#6b7390]#{?pane_active,#[fg=#107dc8#,bold],}#{pane_index}#[fg=#6b7390,nobold]:#{pane_current_command} "

# コマンドライン（display-message / confirm-before の表示）
set -g message-style "fg=#ffffff,bg=#107dc8,bold"
set -g message-command-style "fg=#107dc8,bg=#394260"

# コピーモードの選択範囲と検索マッチ
set -g mode-style "fg=#10151d,bg=#a3aed2"
set -g copy-mode-match-style "fg=#1d2230,bg=#a0a9cb"
set -g copy-mode-current-match-style "fg=#ffffff,bg=#107dc8,bold"

# prefix + q のペイン番号表示、prefix + t の時計
set -g display-panes-colour "#6b7390"
set -g display-panes-active-colour "#107dc8"
setw -g clock-mode-colour "#107dc8"
